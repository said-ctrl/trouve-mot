{% extends 'base.html.twig' %}

{% block title %}RapPendu!{% endblock %}
	{% block stylesheets %}
			<script src="https://cdn.tailwindcss.com"></script>
			<link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet"/>

		{% endblock %}



{% block body %}




    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            color: #333;
            text-align: center;
            padding: 20px;
        }
        .hidden-word {
            color: transparent;
            text-shadow: 0 0 5px rgba(0,0,0,0.5);
        }
        .revealed {
            color: #4CAF50;
            font-weight: bold;
            text-shadow: none;
        }
        #gameForm {
            margin-top: 20px;
        }
        #wordInput {
            padding: 10px;
            font-size: 16px;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
        #foundWords {
            margin-top: 20px;
            font-size: 18px;
            color: #ff5722;
        }
        #score {
            margin-top: 20px;
            font-size: 18px;
            color: #2196F3;
        }
        #confetti {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 9999;
        }
    </style>


<div class="mx-auto container" >


    <h1 class="text-3xl border-2" >Jeu de Masquage de Texte</h1>
    <div id="phraseContainer" class="my-3" >
            <p class="text-2xl my-3"><strong>Artiste :</strong>{{ phrase.author }}</p>

        <p class="text-2xl my-3"><strong>Chanson :</strong>{{ phrase.title }}</p>

        <p class="text-xl mt-3"     id="maskedText">{{ phrase.maskedText }}</p>
    </div>
    <form id="gameForm">
        <label class="text-xl mt-3" for="wordInput">Entrez un mot :</label>
        <input type="text" id="wordInput" name="word">
        <button class="text-black" type="submit" onclick="checkWord()" class="text-xl mt-3">Soumettre</button>
    </form>
    <p class="text-xl mt-3">Mots trouvés : <span id="foundWords"></span></p>
    <p class="text-xl mt-3">Score : <span id="score"></span></p>
    <button class="text-xl mt-3 text-black" type="button" id="novo">Nouvelle Partie</button>
    <canvas id="confetti"></canvas>
    <script>
        const phrase = {{ phrase | json_encode | raw }};

    </script>

    

</div>

{% endblock %}











    {# <h1 class="text-3xl border-2">Jeu de pendurap </h1>
    <p class="text-2xl"><strong>Chanson :</strong>{{ title }}</p>
        <p class="text-2xl"><strong>Artiste :</strong>{{ artist }}</p>
            <p class="text-2xl"><strong>Phrase a deviner :</strong></p>
            <p class="text-2xl">
            {% for mot in mots_devines %}
            {{ mot }}
                
            {% endfor %}
            </p>
                    <p class="text-2xl"><strong>Phrase avec partie manquante :</strong></p>
                    <p class="text-2xl">{{ missing }}</p>

                                        <p class="text-2xl"><strong>Score :</strong>{{ score }}</p>




      
    <form id="form" action="{{'game_index'}}">
        <input type="text" id="guess" placeholder="Entrez votre reponse">
        <button type="submit">Deviner</button>
    </form>
    <p id="message"></p>
    <p id="current-guess"></p>
     #}
     




    